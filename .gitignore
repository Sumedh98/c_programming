# 1. Ignore everything by default
*

# 2. But do NOT ignore directories (so we can see into folders)
!*/

# 3. And do NOT ignore your actual source files
!*.c
!*.h
!*.md
!.gitignore

# 4. Specifically ignore common C build artifacts just in case
*.o
*.out
*.exe